# PHASE 23 BACKLOG — Enhancement Requests Deferred From Phase 22 UAT

Per Phase 22's instruction: only P0/P1/P2 defects against an *existing approved requirement* trigger a code change during Phase 22. Anything else discovered during real UAT — a new feature, a nice-to-have, a "could it also do X" — is recorded here instead, not built. Nothing goes into `SAP_Architecture_Lab/` from this list without a separate, explicit management-approved phase.

This file starts empty because Phase 22's real UAT has not yet run. Each entry, when added, must follow this exact template:

```
## [Date] — [Requested Feature]

- Requested by:
- Business Reason:
- Affected Workflow:
- Priority (Appletree's own judgment, not Claude's):
- Accounting Impact:
- Security Impact:
- Can the existing ERP already handle this some other way? (yes/no + explanation):
- Management Decision Required: (what exactly needs a yes/no from Appletree management)
```

No entry should be added preemptively or speculatively — only when a real UAT session actually surfaces the request.
